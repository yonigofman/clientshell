# clientshell
clientshell injects public runtime config into compiled frontend apps at container startup, then serves them with a typed browser API. Works with static builds from tools like Vite or Webpack without rebuilding for each environment.
